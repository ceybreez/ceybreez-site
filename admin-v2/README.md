# CeyBreez Admin V14 - V10 Full Features Layout

Upload this folder as `admin-v2/`.

Important:
- Keeps the stable V10 `admin.js` logic and V10 Worker API.
- Adds a clean V14 sidebar/dashboard shell.
- Full V10 sections remain available: Inquiry, Booking, Properties, Tours, Services, Reviews, Reports, Page Builder, Settings.
- Worker replacement is NOT required.
- Live `/admin/` should not be touched.

After upload: Ctrl + F5.
