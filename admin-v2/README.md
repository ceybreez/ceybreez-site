# CeyBreez Admin V13 Production Test Build

Upload this folder as `admin-v2/`.

Do not replace Worker.
Do not replace live `/admin/` V10.

Test URL:
https://ceybreez.com/admin-v2/admin.html

This is a clean standalone admin shell. It does not overlay V10 HTML, so there should be no duplicate dashboard, no hidden-space layout issue, and no login panel inside modules.

Uses existing V10 Worker API:
- /api/admin/inquiries
- /api/admin/bookings
- /api/admin/properties
- /api/admin/destinations
- /api/admin/services
- /api/admin/reviews
