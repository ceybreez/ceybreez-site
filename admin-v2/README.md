# CeyBreez Admin V15 - Fixed Router Build

Upload this folder as `admin-v2/`.

Do not replace Worker. Do not touch live `/admin/`.

Fixes:
- Non-dashboard pages rendering inside old V10 wrapper
- Large blank gap above Inquiry / Booking / Availability pages
- Fixed sidebar + correct main content width
- Dashboard remains independent
- V10 feature logic still used
